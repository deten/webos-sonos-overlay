description "Sonos volume overlay background service"

# Start after Luna (webOS service bus) is up so luna-send commands work
start on started luna
stop on stopping luna

respawn
respawn limit 5 60

script
  exec /usr/bin/node /home/root/tv-service.js >> /var/log/tv-service.log 2>&1
end script
